import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'i_producer_property_gateway.dart';

final producerPropertyGatewayProvider = Provider<IProducerPropertyGateway>((
  ref,
) {
  throw UnimplementedError(
    'producerPropertyGatewayProvider: registrar adapter no ProviderScope.',
  );
});
